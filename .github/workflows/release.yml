name: Release

on:
  push:
    tags:
      - v*

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json
      - name: Resolve release version
        shell: pwsh
        run: |
          if ($env:GITHUB_REF_NAME -notmatch '^v\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?$') { throw "Invalid release tag '$env:GITHUB_REF_NAME'." }
          $version = $env:GITHUB_REF_NAME.Substring(1)
          $tagType = (git cat-file -t "refs/tags/$env:GITHUB_REF_NAME").Trim()
          if ($tagType -ne 'tag') { throw "Release tag '$env:GITHUB_REF_NAME' must be annotated." }
          git fetch origin main
          $tagCommit = (git rev-list -n 1 "refs/tags/$env:GITHUB_REF_NAME").Trim()
          git merge-base --is-ancestor $tagCommit origin/main
          if ($LASTEXITCODE -ne 0) { throw "Release tag '$env:GITHUB_REF_NAME' is not reachable from origin/main." }
          $notes = ".\docs\releases\$env:GITHUB_REF_NAME.md"
          if (-not (Test-Path -LiteralPath $notes -PathType Leaf)) { throw "Release notes were not found at '$notes'." }
          "BOOKSPLICE_VERSION=$version" | Out-File -FilePath $env:GITHUB_ENV -Append
          "BOOKSPLICE_RELEASE_NOTES=$notes" | Out-File -FilePath $env:GITHUB_ENV -Append
          "BOOKSPLICE_PRERELEASE=$($version.Contains('-').ToString().ToLowerInvariant())" | Out-File -FilePath $env:GITHUB_ENV -Append
      - run: dotnet restore
      - name: Acquire pinned media tools
        shell: pwsh
        run: |
          .\scripts\Get-MediaTools.ps1 -ManifestPath .\tools\ffmpeg\manifest.json -DestinationRoot .\artifacts\tools\ffmpeg
          $release = (Get-Content .\tools\ffmpeg\manifest.json | ConvertFrom-Json).release
          "BOOKSPLICE_FFMPEG_DIR=$((Resolve-Path ".\artifacts\tools\ffmpeg\$release").Path)" | Out-File -FilePath $env:GITHUB_ENV -Append
      - run: dotnet build --configuration Release --no-restore
      - run: dotnet test --configuration Release --no-build
      - run: dotnet format --verify-no-changes --no-restore
      - name: Build and test release package
        shell: pwsh
        run: |
          .\scripts\Build-Release.ps1 -Version $env:BOOKSPLICE_VERSION -MediaToolDirectory $env:BOOKSPLICE_FFMPEG_DIR
          .\scripts\Test-Release.ps1 -ArchivePath ".\artifacts\release\BookSplice-$env:BOOKSPLICE_VERSION-win-x64.zip" -ChecksumPath ".\artifacts\release\BookSplice-$env:BOOKSPLICE_VERSION-win-x64.zip.sha256"
      - uses: actions/attest-build-provenance@v2
        with:
          subject-path: artifacts/release/BookSplice-*-win-x64.zip
      - uses: softprops/action-gh-release@5113cdc90fd4d541c801c55356214017bf5ae34b # v3
        with:
          body_path: ${{ env.BOOKSPLICE_RELEASE_NOTES }}
          prerelease: ${{ env.BOOKSPLICE_PRERELEASE }}
          draft: true
          overwrite_files: false
          fail_on_unmatched_files: true
          files: |
            artifacts/release/BookSplice-*-win-x64.zip
            artifacts/release/BookSplice-*-win-x64.zip.sha256
            scripts/Test-Release.ps1
